import 'package:flutter/material.dart';

class NukiPinScreen extends StatelessWidget {
  const NukiPinScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
